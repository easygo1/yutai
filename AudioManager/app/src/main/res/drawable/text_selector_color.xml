<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:color="@color/textDown" android:state_pressed="true"></item>  <!-- 点击后的字体颜色 -->
    <item android:color="@color/gray"></item>    <!-- 默认字体颜色 -->
    <item android:color="@color/textDown" android:state_selected="true"></item>
    <item android:color="@color/gray" android:state_selected="false"></item>
</selector>